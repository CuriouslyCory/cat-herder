## 2026-05-08 - US-312
- What was implemented: Verification-only story — no new code. Ran `pnpm test && pnpm lint && pnpm typecheck && pnpm build` and all pass clean. Confirmed production JS bundles in `.next/static/` contain neither "DebugMenu" nor "MapEditor" (tree-shaken via `process.env.NODE_ENV === "production"` guard). Used dev-browser to verify `/` has zero console errors/warnings and `/play` redirects to sign-in with no game-logic errors. "No available adapters" warning is WebGPU fallback from Three.js renderer (explicitly acceptable per AC). "willReadFrequently" canvas warning originated from the external WorkOS sign-in page, not game code (confirmed via grep — no `getImageData` in src/game). 694 tests pass across 33 test files.
- Files changed: prd.json, progress.txt
- **Learnings for future iterations:**
  - Production bundle tree-shaking check: `grep -rl "<Identifier>" .next/static/` is the correct command; trace files in `.next/dev/trace` will contain dev identifiers — only check `.next/static/` for production bundle contents
  - Three.js WebGPU "No available adapters" browser warning is emitted when WebGPU is unavailable (Chromium without GPU) — it is always acceptable, not a game-code issue
  - `willReadFrequently` canvas advice comes from the browser inspector, not game code — confirm by grepping src/ for `getImageData` before investigating further
  - Auth-gated game features (60 FPS, save/load, cat types, swimming) require a live WorkOS session to verify in browser — document this for manual sign-off in CI gate
---

## 2026-05-08 - US-311b
- What was implemented: `src/game/ecs/components/CatScaleAnimation.ts` (new) — `{ type: "CatScaleAnimation", fromScale, toScale, elapsed, duration, destroyOnComplete }` ECS component. `src/game/systems/VisualEffectsSystem.ts` — new `_updateScaleAnimations()` method: queries `CatScaleAnimation + Transform`, advances elapsed, lerps scale on all three Transform axes (scaleX/Y/Z), removes component when complete if `destroyOnComplete=false`, destroys entity after iteration if `destroyOnComplete=true`. `src/game/cats/CatCompanionManager.ts` — `summon()`: Transform now starts at scale 0 (`createTransform(x, y, z, 0, 0, 0, 0)`), adds `createCatScaleAnimation(0, 1, 0.2, false)` for pop-in. `dismiss()`: restructured to skip `world.destroyEntity()` — instead removes CatBehavior (blocks zombie AI processing), removes CuriosityReveal (prevents double-dismiss from CuriositySystem), adds `createCatScaleAnimation(1, 0, 0.2, true)` for pop-out; VisualEffectsSystem destroys the entity when tween completes. Guard added: if entity already has a `destroyOnComplete=true` animation in flight, dismiss() is a no-op. HUD gather progress bar and CuriositySystem hidden terrain fade-in were already implemented (CSS transition + FADE_SPEED=2.0 respectively). `src/game/__tests__/systems/entity-effects.test.ts` (new) — 17 tests: VisualEffectsSystem scale tween (up, down, destroyOnComplete, clamping, uniform axes), CatCompanionManager summon (scale=0 at spawn, CatScaleAnimation added, VFX tweens to 1), dismiss (entity alive, CatScaleAnimation added, removed from companions, event fires, VFX destroys after 0.2s, double-dismiss idempotent, CatBehavior removed), HUD gather bar (cssText includes transition), CuriositySystem opacity (starts at 0, increases after reveal, reaches 1 after 0.5s, getOpacity > 0 after first tick). Existing tests updated: dismiss() no longer immediately destroys entities — assertions changed from `world.isAlive(entity).toBe(false)` to `world.getComponent(entity, "CatBehavior").toBeNull()` in ZoomiesSystem.test.ts, CuriositySystem.test.ts (×2), cat-lifecycle.test.ts, and CatCompanionManager.test.ts. 694 tests pass.
- Files changed: src/game/ecs/components/CatScaleAnimation.ts (new), src/game/systems/VisualEffectsSystem.ts, src/game/cats/CatCompanionManager.ts, src/game/__tests__/systems/entity-effects.test.ts (new), src/game/__tests__/systems/ZoomiesSystem.test.ts, src/game/__tests__/systems/CuriositySystem.test.ts, src/game/__tests__/cats/CatCompanionManager.test.ts, src/game/__tests__/integration/cat-lifecycle.test.ts, prd.json
- **Learnings for future iterations:**
  - Scale tween via Transform: since RenderSystem reads Transform.scaleX/Y/Z each frame and calls sceneManager.updateTransform(), VisualEffectsSystem can tween scale by directly mutating Transform — no new SceneManager API needed
  - Deferred entity destruction: collect entities to destroy in a list during the query loop, then destroy AFTER the loop to avoid mutating the query snapshot mid-iteration
  - Zombie entity protection: after dismiss(), removing CatBehavior prevents CatAISystem/ZoomiesSystem/PounceSystem from processing the animating entity; removing CuriosityReveal prevents CuriositySystem.flushDismissals() from calling dismiss() a second time
  - Double-dismiss guard: check for existing `destroyOnComplete=true` CatScaleAnimation before adding another — prevents double-cleanup when CuriositySystem and ZoomiesSystem both try to dismiss the same expired cat
  - Test contract: whenever dismiss() behavior changes, check ALL test files that assert `world.isAlive(entity)` after a dismiss — they may need updating across integration, unit, and system test suites
  - Inline generic type `getComponent<{ destroyOnComplete: boolean }>` violates the Component constraint (requires `type` field) — always use the concrete component interface as the type parameter
---

## 2026-05-08 - US-311a
- What was implemented: `src/game/ecs/components/ShadowSprite.ts` (new) — `{ type: "ShadowSprite", parentEntity: Entity }` marker component for ground-plane shadow disks. `src/game/systems/VisualEffectsSystem.ts` — refactored into three private methods: `_updateYarnPickups` (existing pulse), `_updateWater` (new: opacity oscillates between 0.6-0.8 at 1.5 rad/s via `setMeshOpacity`), `_updateShadows` (new: tracks player+cat entities via a `Map<Entity,Entity>` shadowMap, creates flat cylinder shadow entities on first encounter, updates Transform XZ each frame, destroys shadow when parent destroyed). `src/game/maps/MapManager.ts` — exported `varyColor(baseHex, seed)` function (Knuth multiplicative hash → ±12 per-channel delta, deterministic per zone position); applied in `createTerrainEntity()` for Grass/Dirt/Stone zones, skipped for Water/Hidden. `src/game/engine/SceneManager.ts` — added `private _grid: THREE.GridHelper | null`, `setTerrainGrid(totalWidth, totalDepth, cellSize)` (creates GridHelper at y=0.01), `removeTerrainGrid()` (scene.remove + geometry/material dispose); `dispose()` now calls `removeTerrainGrid()` before mesh cleanup. `src/game/engine/Game.ts` — `_boot()` calls `sceneManager.setTerrainGrid(TestMap.size.width, TestMap.size.depth, TestMap.cellSize)` after `mapManager.loadMap(TestMap)`. `src/game/__tests__/systems/VisualEffectsSystem.test.ts` (new) — 19 tests: shadow creation for player, shadow component parentEntity ref, shadow Transform at y=0.02 matching parent XZ, no duplicate shadows, shadow tracks moved parent, shadow destroyed with parent, 2 shadows for 2 cats, cat shadow cleanup, combined player+cat shadows; water opacity in [0.59–0.81] range verified over 120 frames; yarn pickup emissive call; `varyColor` determinism, different seeds → different output, ±12 channel bound for 7 seeds, clamp below 0, clamp above 255, grid regression. All 671 tests pass.
- Files changed: src/game/ecs/components/ShadowSprite.ts (new), src/game/systems/VisualEffectsSystem.ts, src/game/maps/MapManager.ts, src/game/engine/SceneManager.ts, src/game/engine/Game.ts, src/game/__tests__/systems/VisualEffectsSystem.test.ts (new), prd.json
- **Learnings for future iterations:**
  - VisualEffectsSystem creates ECS entities (shadow disks) during `update()` — the World handles this safely; RenderSystem picks up new entities with Transform+Renderable on the next frame (1-frame delay is imperceptible)
  - Shadow lifecycle uses a `Map<Entity, Entity>` parent→shadow map: iterate the map to destroy stale shadows, iterate parentEntities to create/update — never iterate both at once to avoid concurrent-modification issues
  - `varyColor` uses Knuth multiplicative hash (`seed * 2654435761 >>> 0`) for even distribution; `% 25 - 12` gives integer delta in [-12, 12] — avoids floating-point HSL conversion entirely
  - GridHelper is NOT a THREE.Mesh, so it cannot be stored in the `meshes: Map<SceneHandle, THREE.Mesh>` map — use a separate `_grid` field and call `scene.add/_remove` directly; dispose pattern: `geometry.dispose() + material.dispose()` (or forEach if material is an array)
  - Water animation: query `WaterTrigger` + `Renderable` together; guard `if (!renderable?.sceneHandle) continue` so test entities without RenderSystem registration are skipped gracefully
  - `setMeshOpacity` must be called in `_updateWater` every frame (not just on state transitions) because the opacity is a continuous sine — RenderSystem doesn't reverse changes, so per-frame updates are the correct model
---

## 2026-05-08 - US-310c
- What was implemented: `src/game/engine/InputManager.ts` — added `onBlur` and `onVisibilityChange` bound handlers. `onBlur` clears `held`, `pressedThisFrame`, and both click flags. `onVisibilityChange` calls `onBlur` when `document.hidden` is true. Both listeners registered in constructor (`canvas.addEventListener("blur", ...)` and `document.addEventListener("visibilitychange", ...)`); both removed in `dispose()`. `src/game/engine/Game.ts` — added `_eventUnsubs: Array<() => void>` field; both `eventBus.on("save:failed", ...)` and `eventBus.on("player:death", ...)` now push their returned Unsubscribe to `_eventUnsubs`; `destroy()` calls all unsubs and clears the array before `eventBus.clear()`. `src/game/__tests__/integration/input-stability.test.ts` (new) — 18 tests across 4 describe blocks: rapid input (duplicate keydowns deduplicate via Set, simultaneous multi-key no-throw, multiple clicks keep flag true, poll() clears single-frame state, held keys survive poll, keyup works after rapid keydown); blur/visibilitychange (clears held keys, clears click flags, visible→no-clear); dispose() (all 6 canvas listeners removed, visibilitychange removed from document, double-dispose safe); EventBus cleanup (unsub stops handler, unsubs+clear doesn't throw, idempotent unsub). All 652 tests pass.
- Files changed: src/game/engine/InputManager.ts, src/game/engine/Game.ts, src/game/__tests__/integration/input-stability.test.ts (new), prd.json
- **Learnings for future iterations:**
  - InputManager uses `document.addEventListener("visibilitychange", ...)` as a global — tests must `vi.stubGlobal("document", docMock)` BEFORE constructing InputManager, otherwise the global is Node's undefined `document`
  - Mock canvas pattern: `listeners = new Map<string, AnyHandler>()` + `addEventListener: vi.fn((type, handler) => listeners.set(type, handler))` + `fire(type, event?)` — lets tests invoke handlers directly without DOM events
  - `document.hidden` in the visibilitychange handler reads the global at fire-time, not at construction-time — use `Object.defineProperty(docMock, "hidden", { get: () => true, configurable: true })` before `docMock.fire("visibilitychange")` to simulate hidden state
  - Keydown handler calls `e.preventDefault()` for Space and Arrow keys — mock keydown events must include `preventDefault: vi.fn()` to avoid TypeError
  - Game.ts `_eventUnsubs` follows the same pattern as `Persistence._unsubs` (push from `eventBus.on()` return value, call all in `destroy()` before `eventBus.clear()`)
---

## 2026-05-08 - US-310b
- What was implemented: `src/game/types.ts` — added `player:death` event to GameEvent union. `src/game/systems/OxygenSystem.ts` — emits `player:death` when `player.health` reaches 0 after hp drain. `src/game/engine/Game.ts` — added `_onPlayerDeath(entity)` private method (resets health to maxHealth, removes SwimmingState + OxygenState, re-enables gravity, teleports to map spawn point); wired via `eventBus.on("player:death", ...)` in constructor. Inventory-full feedback (GatheringSystem + HUD) was already fully implemented — tests confirm it. Save-while-swimming works via PhysicsEngine initial-overlap detection (first `step()` call with `activeOverlaps` empty emits `trigger:enter` for any overlap). `src/game/__tests__/integration/death-respawn.test.ts` (new) — 8 tests: death event emitted, health reset, swimming components removed, no double-death; inventory full blocks gather, no progress ticks; save-in-water: player inside trigger → SwimmingState added; player outside trigger → not added. All 634 tests pass.
- Files changed: src/game/types.ts, src/game/systems/OxygenSystem.ts, src/game/engine/Game.ts, src/game/__tests__/integration/death-respawn.test.ts (new), prd.json
- **Learnings for future iterations:**
  - Death respawn handler in Game.ts uses `eventBus.on("player:death", ...)` registered in the constructor (after all systems created, same area as `save:failed` handler)
  - EventBus.emit() is synchronous — when OxygenSystem emits `player:death`, the Game.ts handler fires immediately in the same call, removing OxygenState before the loop continues; no re-entry guard needed
  - PhysicsEngine initial-overlap detection: `activeOverlaps` starts empty → all trigger overlaps on the first `step()` emit `trigger:enter`; spawning a player inside a water zone automatically triggers SwimmingState addition on the first physics step
  - Inventory-full feedback was already wired end-to-end (GatheringSystem → isInventoryFull() → Game.buildHUDState → HUD.setInventory); no code changes needed, only test coverage
  - `spawnSwimmingPlayer(world)` from entityFactories adds both SwimmingState and OxygenState — use it for oxygen/death tests to avoid duplicating setup
---

## 2026-05-08 - US-310a
- What was implemented: `src/game/types.ts` — added `cat:place:failed` event to GameEvent union. `src/game/engine/PhysicsEngine.ts` — added `isPositionOccupied(x, y, z, halfExtents)` public method (AABB vs static-body overlap check, ignores triggers and dynamic bodies). `src/game/cats/CatCompanionManager.ts` — enhanced `summon()` with auto-raise: computes `surfaceY + halfHeight` as before, then tries up to 3 positions (0u, 0.5u, 1.0u above) via `isPositionOccupied()`; emits `cat:place:failed` and returns null if all 3 are blocked; importantly, yarn deduction and auto-dismiss only happen AFTER a valid Y is found. `src/game/__tests__/integration/terrain-safety.test.ts` (new) — 12 tests: player push-out from embedded static box, boundary wall blocks movement, `isPositionOccupied` true/false cases (overlap, above, beside, trigger-ignored, dynamic-ignored), auto-raise places at default Y when clear, raises 0.5u on 2nd attempt, raises 1.0u on 3rd attempt, returns null + emits event after all 3 fail, no yarn deducted on failure. `src/game/__tests__/integration/loaf-platform.test.ts` — added dismiss-while-standing test: player rests on Loaf, Loaf body removed via `removeBody()`, player falls safely to floor. All 626 tests pass.
- Files changed: src/game/types.ts, src/game/engine/PhysicsEngine.ts, src/game/cats/CatCompanionManager.ts, src/game/__tests__/integration/terrain-safety.test.ts (new), src/game/__tests__/integration/loaf-platform.test.ts, prd.json
- **Learnings for future iterations:**
  - `EventBus` uses `.on(type, handler)` not `.subscribe()` — always check EventBus API before writing subscriber tests
  - `isPositionOccupied` must ignore trigger bodies and non-static bodies — only solid static bodies constitute "occupied" geometry for placement purposes
  - Auto-raise must happen BEFORE yarn deduction and auto-dismiss so partial side-effects are avoided when placement fails
  - `getCatHalfExtents` is a module-level (unexported) function in CatCompanionManager.ts — it's callable from `summon()` without export; calling it twice (once for auto-raise, once for the physics body) is acceptable since it's pure computation
  - PhysicsEngine push-out for circle-inside-box: `newPos.x = box.x + half.x + radius` pushes all the way to the +X exterior edge — test asserts the player is outside the box in at least one axis (rather than a specific axis) to survive future push-direction changes
---

## 2026-05-08 - US-309
- What was implemented: `src/game/config.ts` — exported `RESOURCE_CONFIGS` constant (Grass/Sticks/Water gather times, yields, respawn timers) extracted from inline consts in `Game.ts spawnTestMapResourceNodes()`. `Game.ts` updated to import and use `RESOURCE_CONFIGS` instead of local consts. `src/game/__tests__/systems/resource-balance.test.ts` — 22 regression tests: direct config value assertions (gatherTime, yieldAmount, respawnTime per type), behavioral tests (gather completes in correct frames, cooldown applied after gather, HUD label format "+1 Type", inventory cap enforcement, resource:gathered event). All 613 tests pass.
- Files changed: src/game/config.ts (RESOURCE_CONFIGS + ResourceNodeConfig interface added), src/game/engine/Game.ts (import + cfg usage updated), src/game/__tests__/systems/resource-balance.test.ts (new), prd.json
- **Learnings for future iterations:**
  - Inline `const` configs inside private methods are untestable directly — extract to `config.ts` as a named export so regression tests can import and assert against canonical values (same pattern as CONFIG for movement values)
  - `as const satisfies Record<string, ResourceNodeConfig>` gives both literal type inference AND structural type checking in one annotation
  - Resource balance test file belongs in `src/game/__tests__/systems/` (gathering is a system-layer concern, not engine)
  - Behavioral timing tests use `gatherTime * 60 + 1` frames (not hardcoded frame counts) so tests stay coherent if the DT constant changes
---

## 2026-05-08 - US-308
- What was implemented: `src/game/__tests__/cats/cat-balance.test.ts` — 23 regression tests asserting GDD-tuned cat parameters. Covers: LoafDefinition (yarnCost=1, height=0.75u, colliderHeight=0.75u, stacking 2×=1.5u / 3×=2.25u, effectType=terrain), ZoomiesDefinition (yarnCost=2, speedMultiplier=2.0, trailLength=6, duration=8s, effectType=movement), CuriosityCatDefinition (yarnCost=2, revealRadius=5, duration=20s, effectType=utility), PounceDefinition (yarnCost=3, launchImpulse=3.5, airControlFactor=0.7, effectType=launch, ballistic apex > 0), GameState initial yarn=10, maxInventoryCapacity=10, CONFIG.maxActiveCats=3. All cat definition values matched GDD targets — no definition files changed. 591 tests pass (23 new US-308 tests).
- Files changed: src/game/__tests__/cats/cat-balance.test.ts (new), prd.json
- **Learnings for future iterations:**
  - CatDefinition.behavior.params is typed as `Record<string, number | string | boolean> | undefined` — access with optional chaining and cast to specific type (e.g. `as number`) for arithmetic tests
  - CuriosityCat uses `meshConfig.size` (sphere) not `meshConfig.dims` — don't assume all cats use dims
  - Balance pass tests for cats belong in `src/game/__tests__/cats/` to match source layout (cats are not engine-layer concerns)
  - The Pounce ballistic apex test (v²/2|g|) uses CONFIG.gravity — test stays coherent even if gravity changes because it re-derives from CONFIG

---

## 2026-05-08 - US-306
- What was implemented: `src/game/ui/NavigationOverlay.ts` — 400×400 HTML5 canvas minimap overlay. `MapManager.getMapData()` public method added. `WorldLike`/`MapManagerLike` local interfaces (avoid Three.js transitive deps in tests). `open()`/`close()`/`toggle()`/`isVisible()`/`update(dt)`/`dispose()` public API. Capture-phase keydown listener (`document.addEventListener("keydown", …, true)`) calls `stopImmediatePropagation()` on all keys while visible (blocks InputManager bubble-phase listeners = movement suppressed). M and Escape close the overlay. `worldToPixel()` exposed publicly for tests. Renders: terrain cells (Grass/Dirt/Stone/Water, Hidden skipped), resource node dots by ResourceType color, cat circles (purple), player dot + direction triangle (#ff6b35). Game.ts: imports `GameAction`, adds `navigationOverlay` field, opens on `isActionPressed(ToggleMap) && !mapEditor?.isActive()`, calls `update(realDt)` and `dispose()`. 554 tests pass (28 new US-306 tests).
- Files changed: src/game/ui/NavigationOverlay.ts (new), src/game/__tests__/engine/NavigationOverlay.test.ts (new), src/game/maps/MapManager.ts (getMapData added), src/game/engine/Game.ts (import + field + init + loop + dispose), prd.json
- **Learnings for future iterations:**
  - `World.getComponent<T extends Component>()` returns `T | null` (not `T | undefined`) — `WorldLike` interface must use the same generic constraint or TypeScript rejects `World` as a compatible argument
  - Capture-phase keydown (`addEventListener("keydown", fn, true)`) with `stopImmediatePropagation()` is the correct way to block bubble-phase InputManager listeners; `stopPropagation()` alone does NOT block sibling document listeners
  - For player-facing overlays (not dev-gated), wire opening in Game.ts frame loop via `isActionPressed()` + guard against editor active; the overlay manages its own closing via the capture handler
  - Canvas element in tests: `makeMockEl()` must have `getContext: vi.fn(() => makeMockCtx2d())` — the 2D context mock needs all drawing methods (`clearRect`, `fillRect`, `arc`, `fill`, `stroke`, `beginPath`, `closePath`, `moveTo`, `lineTo`, `save`, `restore`, `translate`, `rotate`, `fillText`)
  - When a second overlay instance is created inside a test, `createdElements` indices shift by 2 per instance (div + canvas) — be precise about which index to assert against
  - `dispose()` must reset `_visible = false` so `isVisible()` returns the correct state after disposal

---

## 2026-05-08 - US-305
- What was implemented: Map editor save/load/play. New `src/game/maps/MapDataSchema.ts` with Zod v4 schema (`mapDataSchema`, `terrainCellSchema`, `spawnPointSchema`) mirroring MapData interface. `MapManagerLike` interface added to MapEditor.ts (avoids circular import). 5th constructor parameter `mapManager: MapManagerLike | null = null` added. "File" section added to `_buildPanel()` with Save, Load (file input + button), Play buttons and red error display. Three new public methods: `saveMap()` (Blob + anchor download), `loadFromFile(file: File)` (async, Zod-validates, shows error on failure), `playMap()` (calls mapManager.loadMap then disable()). `_showError()` private helper. `dispose()` nulls `_errorDisplay` and `_fileInput`. `Game.ts` passes `this.mapManager` as 5th arg. 526 tests pass (16 new US-305 tests).
- Files changed: src/game/maps/MapDataSchema.ts (new), src/game/maps/MapEditor.ts, src/game/engine/Game.ts, src/game/__tests__/engine/MapEditor.test.ts, prd.json
- **Learnings for future iterations:**
  - `MapManagerLike` interface in MapEditor.ts (1-method subset) avoids importing MapManager and keeps the dependency graph clean
  - Mock `createElement` in tests returns `MockEl` which lacks `click()` — must add `click: vi.fn()` to the `MockEl` interface and `makeMockEl()` factory, otherwise `saveMap()` throws "a.click is not a function"
  - `loadFromFile` is `async` — tests must `await` it; use `{ text: vi.fn().mockResolvedValue(...) }` as the File mock
  - Zod `safeParse` on invalid enum values returns `success: false` reliably; `result.error.issues[0]?.message` gives the user-facing error string
  - The `_fileInput` element is created via `document.createElement("input")` (goes through the mock), so it lands in `createdElements` — its `.click()` method is also on `MockEl` so `_fileInput?.click()` from Load button handler works cleanly

---

## Codebase Patterns
- US-304 added `EditorToolMode = 'move' | 'delete'` as a third, mutually exclusive tool selection alongside terrain/entity tools — setting any one clears the others
- Move tool drags the *real* object (no ghost) — updates `obj.x/z` and `updateTransform` in real-time on mousemove; mouseup sets `_suppressNextClick` to prevent the click handler from also firing
- `_findPointObjectAt(gx, gz)` returns a discriminated `MovingObject` union that `_removeFoundObject` and `_updateObjectPosition` can switch on; zone objects (water, hidden terrain) are excluded as they're not point-clickable
- Keyboard shortcuts M/D/1-9 call `e.stopPropagation()` while editor is active to prevent game InputManager from seeing them
- `excludeHandles` set in mousemove now includes both `_ghostHandle` and the moving object's handle so screenToWorld hits ground geometry during drag
- Entity tools in MapEditor use a separate `_selectedEntityTool: EntityTool | null` field — selecting one clears the other (terrain ↔ entity mutual exclusion via `selectTool`/`selectEntityTool`)
- Entity markers use `geometry: "sphere"` (vs terrain blocks which use `geometry: "box"`) — gives immediate visual distinction between entity markers and terrain tiles
- `RESOURCE_RESPAWN_DEFAULTS` is exported so tests can assert against it directly without magic numbers
- Hidden terrain zones reuse the same mousedown/mouseup drag pattern as water zones — just a separate set of state vars (`_hiddenDragStart`, `_hiddenDragGhost`) feeding `createHiddenTerrainZone()`
- Player spawn uniqueness: `_placePlayerSpawn()` removes the old handle via `sceneManager.removeMesh()` before assigning the new one — tests verify `removeMesh` is called with the prior handle
- Entity config section (`_entityConfigSection`) is cleared via `textContent = ""` and rebuilt by `_updateEntityConfigSection()` on each `selectEntityTool()` call — avoids stale DOM refs

## 2026-05-08 - US-304
- What was implemented: `EditorToolMode = 'move' | 'delete'` meta-tool modes. `setEditorToolMode()`, `getEditorToolMode()`, `deleteObjectAtPosition()` public API. Move tool: mousedown picks object via `_findPointObjectAt`, mousemove calls `_updateObjectPosition` (updates obj.x/z + mesh transform in real-time), mouseup finalises + suppressNextClick. Delete tool: click handler routes to `deleteObjectAtPosition` → `_findPointObjectAt` → `_removeFoundObject`. Keyboard: M/D toggle modes (stopPropagation while active), 1-4 terrain palette, 5-9 entity palette. Panel: Move [M] and Delete [D] buttons added to a "Tools" section. All tools mutually exclusive. 510 tests pass.
- Files changed: src/game/maps/MapEditor.ts, src/game/__tests__/engine/MapEditor.test.ts, prd.json
- **Learnings for future iterations:**
  - Move drag on real objects (no ghost) is simpler than ghost-based drag and provides direct visual feedback — mousemove updates x/z + transform immediately, mouseup just clears `_movingObject`
  - `_findPointObjectAt` returns a discriminated union so callers don't need to check collection type; keep zones (water, hidden terrain) out of this lookup since they're defined by area not a single click point
  - When building `excludeHandles` for screenToWorld, combine ghost handle AND moving object handle in a single Set so both are excluded during drag raycasts
  - `e.stopPropagation()` (not just `preventDefault`) is needed on M/D/1-9 to prevent game InputManager (which also listens on document) from seeing those keys while editor is active
---

## 2026-05-08 - US-303
- MapEditor is dev-gated via `process.env.NODE_ENV === 'production'` guard in constructor — same pattern as DebugMenu
- DOM overlays (banner, panel) append to `container.parentElement ?? document.body`; container is the canvas element
- SceneManager is abstracted behind a local `SceneManagerLike` interface in MapEditor.ts to avoid importing Three.js types and keep tests clean
- Test DOM mocks: use a `createdElements[]` queue (each `createElement` call returns a fresh MockEl) so banner (index 0) and panel (index 1) are independently testable
- For tests that need SceneManager, always use a SEPARATE container mock so event listener calls don't bleed between test setups
- `as any` is acceptable for internal interface mocks in tests (no eslint suppression needed — rule not configured for this pattern)
- Block meshes use unit cubes (dims: [1,1,1]) with scale.y = height — avoids geometry recreation on height changes; updateTransform(handle, {y: h/2}, rot, {y: h}) is all that's needed
- `SceneManagerLike` in MapEditor.ts must stay in sync with the real SceneManager API (setMeshEmissive, setMeshColor, etc.) — extend both together
- Water zone drag uses mousedown/mouseup pair; a `_suppressNextClick` flag prevents the `click` handler from double-firing after a drag completes

---

## 2026-05-08 - US-302a
- What was implemented: Terrain tool palette side panel in MapEditor. Left panel (160px wide, dark background) with 4 terrain type buttons (Grass/Dirt/Stone/Water), each with color swatch matching TERRAIN_COLORS. selectTool()/getSelectedTool() public API. placeBlock() places EditorBlocks at grid-snapped (Math.round) positions. Ghost preview mesh (opacity 0.4) follows mouse via SceneManager.screenToWorld + mousemove listener on canvas. SceneManager passed as optional 4th constructor arg (SceneManagerLike internal interface). Game.ts updated to pass sceneManager to MapEditor.
- Files changed: src/game/maps/MapEditor.ts, src/game/engine/Game.ts, src/game/__tests__/engine/MapEditor.test.ts
- **Learnings for future iterations:**
  - The existing MapEditor test setup used a single `mockCreatedEl` for ALL createElement calls — had to redesign to a queue (createdElements[]) so banner[0] and panel[1] are independently tracked
  - SceneManager mouse handlers must use a SEPARATE container per editor instance in tests; shared container accumulates handlers from multiple editor instances causing wrong handler to be found
  - Ghost is only created on mousemove (lazy), so ghost preview tests must fire mousemove before testing removeMesh
  - `SceneManagerLike` is intentionally unexported — internal interface for clean boundary; callers pass `as any` in tests
---

## 2026-05-08 - US-302b
- What was implemented: Block selection (click-to-select when no tool active), visual highlight via setMeshEmissive, properties panel showing X/Z position + type dropdown + height input. updateSelectedBlockType() changes mesh color via setMeshColor. updateSelectedBlockHeight() updates scale.y (no mesh recreation). deleteSelectedBlock() removes from state + calls removeMesh. Delete key handler in keydown. EditorWaterZone type + createWaterZone() with normalised bounds. Water zone drag via mousedown/mouseup pair with _suppressNextClick flag. `_cancelWaterDrag()` cleans up drag ghost and state.
- Files changed: src/game/maps/MapEditor.ts, src/game/__tests__/engine/MapEditor.test.ts
- **Learnings for future iterations:**
  - Block meshes must use unit cubes with scale.y=height so height can be updated in-place via updateTransform — geometry-backed height cannot be changed without mesh recreation
  - `SceneManagerLike` interface must be extended whenever new SceneManager methods are used; also update `makeMockSceneManager` in tests with the new vi.fn() stubs
  - When click and drag share the same DOM event path, use a `_suppressNextClick` boolean to prevent double-firing after a drag completes in mouseup
  - `document.createElement("select")` is typed as `HTMLSelectElement` by TypeScript's lib.dom.d.ts, so no cast is needed; at runtime tests return MockEl but JavaScript allows setting arbitrary properties on it
  - dispose() must clean up ALL registered container listeners (mousemove, mousedown, mouseup, click) to avoid leaks
---

## 2026-05-08 - US-303
- What was implemented: Entity tool palette section (5 tools: Player Spawn, Cat Spawn, Resource Node, Hidden Terrain, Yarn Pickup) added to MapEditor side panel. `EntityTool` union type exported. `EditorPlayerSpawn`, `EditorCatSpawn`, `EditorResourceNode`, `EditorHiddenTerrainZone`, `EditorYarnPickup` interfaces exported. `placeEntity()` dispatches to type-specific placement methods. Player spawn enforces uniqueness (replaces existing, removes old mesh). Resource nodes store type + respawnTime from exported `RESOURCE_RESPAWN_DEFAULTS` (Grass 30s, Sticks 45s, Water 60s). Hidden terrain uses same mousedown/mouseup drag as water zones. Entity markers render as spheres (vs box tiles). `_updateEntityConfigSection()` dynamically builds config controls per tool. All entity handles cleaned up in `dispose()`. `disable()` calls `_cancelHiddenDrag()`.
- Files changed: src/game/maps/MapEditor.ts, src/game/__tests__/engine/MapEditor.test.ts
- **Learnings for future iterations:**
  - Mutual exclusion between terrain and entity tool: `selectTool()` clears `_selectedEntityTool` and vice versa — both update their respective button highlight maps
  - Entity config section content: cleared via `textContent = ""` then rebuilt each time `selectEntityTool()` is called; refs to config inputs (respawnInput, etc.) are local to builder functions, not stored as fields
  - Hidden terrain drag exactly mirrors water zone drag code — only difference is the color constant and the target collection (`_hiddenTerrainZones` vs `_editorWaterZones`)
  - Exporting `RESOURCE_RESPAWN_DEFAULTS` lets tests assert against canonical values without duplicating magic numbers
  - For dispose() tests with mesh cleanup: always `mockClear()` the `removeMesh` spy AFTER the entity is placed (not before) to isolate just the disposal calls
---

## 2026-05-08 - US-307
- What was implemented: `src/game/__tests__/engine/config-balance.test.ts` — 14 regression tests asserting all GDD-tuned movement, jump, and swimming config values from `CONFIG` in config.ts. Covers: walkSpeed (4.5), acceleration (0.3), deceleration (0.2), airControlFactor (0.7), jumpApex (1.2), gravity (-12), derived jumpImpulse (~5.367), coyoteFrames (5), jumpBufferFrames (5), swimSpeedSurface (3.2), swimSpeedDive (2.0), swimSpeedAscend (2.5), maxActiveCats (3), and GameState initial yarn (10). All config values already matched the GDD targets — no config.ts changes needed.
- Files changed: src/game/__tests__/engine/config-balance.test.ts (new), prd.json
- **Learnings for future iterations:**
  - CONFIG is `Object.freeze({...BASE_CONFIG})` — tests can safely import and assert against it without mutation concerns
  - The derived `jumpImpulse` field is computed at module load (after BASE_CONFIG literal) and set via `BASE_CONFIG.jumpImpulse = Math.sqrt(...)` then frozen into CONFIG — tests use `toBeCloseTo` for the float comparison
  - Balance pass tests belong in `src/game/__tests__/engine/` alongside other engine-layer tests (config.ts is engine-level, not a system)
---
