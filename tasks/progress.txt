## Codebase Patterns
- Three.js is installed as a dep (not devDep); @types/three is devDep
- next/dynamic with ssr:false must be used in a 'use client' component (GameLoader.tsx) — Next.js 16+ requires this
- Game modules live under src/game/; never import from src/app/ or src/server/ from within src/game/
- WorkOS auth: withAuth({ ensureSignedIn: true }) in server components; pass only {id, firstName, email} to client
- Config: CONFIG is frozen (Object.freeze), runtimeConfig is mutable deep copy for debug menu
- ECS Entity is a numeric type alias (not a class); query() results are cached and invalidated on add/remove
- SceneManager is the only module that may import directly from 'three' (besides CameraController)
- PhysicsEngine uses downward raycasts for ground detection with 0.05u snap tolerance
- MovementSystem reads from runtimeConfig (not CONFIG) so debug tweaks apply at runtime

---

## 2026-04-29 - US-018
- Implemented UIManager and HUD as plain DOM modules (no React in the engine loop)
- UIManager creates an `position:absolute;inset:0` overlay div appended to canvas.parentElement; promotes parent to `position:relative` if static
- HUD renders: 3 heart spans (♥, bottom-left) always visible; FPS counter div (top-right) only when `process.env.NODE_ENV === 'development'`
- FPS computed via 60-sample rolling average of per-frame dt values, updated every frame
- Files: src/game/ui/UIManager.ts (rewritten), src/game/ui/HUD.ts (new)
- pnpm typecheck passes
- **Learnings for future iterations:**
  - UIManager overlay uses `inset:0` (shorthand for top/right/bottom/left:0) — requires parent to be a positioning context; we detect and promote it automatically
  - `process.env.NODE_ENV` is replaced at Next.js build time, so dev-only DOM elements cost nothing in production bundles
  - HUD dispose() only nulls refs; overlay removal is UIManager's responsibility — keeps concerns separated

---

## 2026-04-29 - US-001
- Verified: Three.js installed, GameCanvas.tsx 'use client' component, GameLoader with next/dynamic ssr:false, game.destroy() on unmount, withAuth on /play page, Game.ts stub renders colored box on ground plane
- Files: src/app/play/_components/GameCanvas.tsx, src/app/play/_components/GameLoader.tsx, src/app/play/page.tsx, src/game/engine/Game.ts
- pnpm typecheck and pnpm build pass
- **Learnings for future iterations:**
  - The Game.ts is currently a US-001 stub (directly uses THREE) — US-017 will replace it with the full orchestrator wiring ECS/systems
  - GameLoader.tsx is the 'use client' wrapper required by Next.js 16+ for next/dynamic; play/page.tsx is a server component that calls withAuth
  - GameCanvas creates the canvas element imperatively (not via JSX) to control exact sizing before Three.js init
---

## 2026-04-30 - US-104
- Implemented CatCompanionManager with summon(), dismiss(), getCatalog(), getActiveCompanions()
- Created GameState (src/game/engine/GameState.ts): reactive yarn tracking via onYarnChange(); deductYarn() returns bool for validation; starts at 10 yarn
- Created CatBehavior ECS component (src/game/ecs/components/CatBehavior.ts): catType, state (Idle|Active|Cooldown|Expired), stateTimer, ownerId, yarnCost
- Created CatCompanionManager (src/game/cats/CatCompanionManager.ts): validates yarn, position (via MapManager.getTerrainAt), auto-dismisses oldest on limit breach; yarn refunded only when state !== 'Expired'
- Added cat:summoned and cat:dismissed events to GameEvent discriminated union (types.ts)
- Added maxActiveCats: 3 to GameConfig and BASE_CONFIG (config.ts)
- pnpm typecheck and pnpm lint both pass
- **Learnings for future iterations:**
  - CatCompanionManager takes MapManager by reference for terrain validation — Game.ts will need to wire it up when integrating (US-105+)
  - GameState is a standalone class (not a singleton); it must be instantiated once in Game.ts and passed to CatCompanionManager
  - companions Map preserves insertion order (ES2015 guarantee), so getActiveCompanions()[0] is always the oldest — safe for auto-dismiss
  - Cat entity collider uses layer 1 / mask 1 (same as player) so CollisionSystem detects overlap; per-cat type stories (US-106-109) will refine collider config
---

## 2026-04-30 - US-108
- Implemented Curiosity Cat reveal mechanic: hidden terrain becomes visible when a Curiosity Cat is summoned within 5u
- Created HiddenTerrain component (src/game/ecs/components/HiddenTerrain.ts): isRevealed flag + revealCount (supports overlapping cats)
- Created CuriosityReveal component (src/game/ecs/components/CuriosityReveal.ts): revealRadius + revealedEntities[] list
- Created CuriositySystem (src/game/systems/CuriositySystem.ts): Idle→Active reveals terrain, ticks stateTimer, Expired hides terrain + auto-dismisses
- Updated MapManager: Hidden-type terrain entities get opacity:0 (fully transparent) + HiddenTerrain tag; receiveShadow/castShadow disabled for hidden tiles
- Updated CatCompanionManager: effectType='utility' cats get CuriosityReveal component attached at summon time
- Added hidden:terrain:revealed event to GameEvent discriminated union (types.ts)
- Wired CuriositySystem into Game.ts (after ZoomiesSystem in the fixed-step loop)
- pnpm typecheck and pnpm lint both pass
- **Learnings for future iterations:**
  - HiddenTerrain revealCount pattern handles multiple overlapping Curiosity cats: terrain stays visible as long as any cat covers it
  - CuriositySystem needs SceneManager access (injected) to call setMeshOpacity() — this is the only system besides CatPlacementSystem that holds a SceneManager ref
  - Hidden terrain's Renderable is created normally by MapManager (opacity:0); RenderSystem registers it as a mesh. CuriositySystem then calls setMeshOpacity(handle, 1) using the sceneHandle stored by RenderSystem. This means reveal only works after the first RenderSystem frame — not a problem in practice since the cat is placed by the user.
  - Collider collisionMask is mutable on the component object; CuriositySystem toggles mask 0↔1 directly without destroying/recreating the entity
---

## 2026-05-05 - US-201
- Extended GameState with path-based API: get<T>(path), set<T>(path, value), onChange<T>(path, callback), serialize(), restore()
- Added internal InternalState object with player/world/session sub-objects; session excluded from serialize()
- Preserved all existing methods (addYarn, deductYarn, onYarnChange, addResource, hasInventorySpace, onInventoryChange) as wrappers
- Added isDirty / clearDirty() for auto-save tracking
- Added validation in set() for player.yarn (>=0), player.stats.health (0..maxHealth), player.stats.level (0-10 integer), player.stats.maxHealth (>0)
- restore() snapshots old values before hydrating, then fires all registered path listeners with correct (newVal, oldVal)
- Files changed: src/game/engine/GameState.ts, src/game/__tests__/engine/GameState.test.ts
- All 167 tests pass; pnpm typecheck/lint/build all clean
- **Learnings for future iterations:**
  - TypeScript won't allow `this._state as Record<string,unknown>` directly — must go through `unknown` first: `as unknown as Record<string,unknown>`
  - addResource() must clone the inventory array before mutating (to give set() a new reference and properly trigger onChange callbacks)
  - onYarnChange/onInventoryChange emit immediately (existing contract) — the path-based onChange() does NOT emit immediately; wrappers call listener(currentVal) before registering
  - SaveData interface lives in GameState.ts for US-201; US-202 will formalize it in src/game/state/SaveData.ts with Zod validation
---
