## Codebase Patterns
- Three.js is installed as a dep (not devDep); @types/three is devDep
- next/dynamic with ssr:false must be used in a 'use client' component (GameLoader.tsx) — Next.js 16+ requires this
- Game modules live under src/game/; never import from src/app/ or src/server/ from within src/game/
- WorkOS auth: withAuth({ ensureSignedIn: true }) in server components; pass only {id, firstName, email} to client
- Config: CONFIG is frozen (Object.freeze), runtimeConfig is mutable deep copy for debug menu
- ECS Entity is a numeric type alias (not a class); query() results are cached and invalidated on add/remove
- SceneManager is the only module that may import directly from 'three' (besides CameraController)
- PhysicsEngine uses downward raycasts for ground detection with 0.05u snap tolerance
- MovementSystem reads from runtimeConfig (not CONFIG) so debug tweaks apply at runtime

---

## 2026-04-29 - US-018
- Implemented UIManager and HUD as plain DOM modules (no React in the engine loop)
- UIManager creates an `position:absolute;inset:0` overlay div appended to canvas.parentElement; promotes parent to `position:relative` if static
- HUD renders: 3 heart spans (♥, bottom-left) always visible; FPS counter div (top-right) only when `process.env.NODE_ENV === 'development'`
- FPS computed via 60-sample rolling average of per-frame dt values, updated every frame
- Files: src/game/ui/UIManager.ts (rewritten), src/game/ui/HUD.ts (new)
- pnpm typecheck passes
- **Learnings for future iterations:**
  - UIManager overlay uses `inset:0` (shorthand for top/right/bottom/left:0) — requires parent to be a positioning context; we detect and promote it automatically
  - `process.env.NODE_ENV` is replaced at Next.js build time, so dev-only DOM elements cost nothing in production bundles
  - HUD dispose() only nulls refs; overlay removal is UIManager's responsibility — keeps concerns separated

---

## 2026-04-29 - US-001
- Verified: Three.js installed, GameCanvas.tsx 'use client' component, GameLoader with next/dynamic ssr:false, game.destroy() on unmount, withAuth on /play page, Game.ts stub renders colored box on ground plane
- Files: src/app/play/_components/GameCanvas.tsx, src/app/play/_components/GameLoader.tsx, src/app/play/page.tsx, src/game/engine/Game.ts
- pnpm typecheck and pnpm build pass
- **Learnings for future iterations:**
  - The Game.ts is currently a US-001 stub (directly uses THREE) — US-017 will replace it with the full orchestrator wiring ECS/systems
  - GameLoader.tsx is the 'use client' wrapper required by Next.js 16+ for next/dynamic; play/page.tsx is a server component that calls withAuth
  - GameCanvas creates the canvas element imperatively (not via JSX) to control exact sizing before Three.js init
---

## 2026-04-30 - US-104
- Implemented CatCompanionManager with summon(), dismiss(), getCatalog(), getActiveCompanions()
- Created GameState (src/game/engine/GameState.ts): reactive yarn tracking via onYarnChange(); deductYarn() returns bool for validation; starts at 10 yarn
- Created CatBehavior ECS component (src/game/ecs/components/CatBehavior.ts): catType, state (Idle|Active|Cooldown|Expired), stateTimer, ownerId, yarnCost
- Created CatCompanionManager (src/game/cats/CatCompanionManager.ts): validates yarn, position (via MapManager.getTerrainAt), auto-dismisses oldest on limit breach; yarn refunded only when state !== 'Expired'
- Added cat:summoned and cat:dismissed events to GameEvent discriminated union (types.ts)
- Added maxActiveCats: 3 to GameConfig and BASE_CONFIG (config.ts)
- pnpm typecheck and pnpm lint both pass
- **Learnings for future iterations:**
  - CatCompanionManager takes MapManager by reference for terrain validation — Game.ts will need to wire it up when integrating (US-105+)
  - GameState is a standalone class (not a singleton); it must be instantiated once in Game.ts and passed to CatCompanionManager
  - companions Map preserves insertion order (ES2015 guarantee), so getActiveCompanions()[0] is always the oldest — safe for auto-dismiss
  - Cat entity collider uses layer 1 / mask 1 (same as player) so CollisionSystem detects overlap; per-cat type stories (US-106-109) will refine collider config
---

## 2026-04-30 - US-108
- Implemented Curiosity Cat reveal mechanic: hidden terrain becomes visible when a Curiosity Cat is summoned within 5u
- Created HiddenTerrain component (src/game/ecs/components/HiddenTerrain.ts): isRevealed flag + revealCount (supports overlapping cats)
- Created CuriosityReveal component (src/game/ecs/components/CuriosityReveal.ts): revealRadius + revealedEntities[] list
- Created CuriositySystem (src/game/systems/CuriositySystem.ts): Idle→Active reveals terrain, ticks stateTimer, Expired hides terrain + auto-dismisses
- Updated MapManager: Hidden-type terrain entities get opacity:0 (fully transparent) + HiddenTerrain tag; receiveShadow/castShadow disabled for hidden tiles
- Updated CatCompanionManager: effectType='utility' cats get CuriosityReveal component attached at summon time
- Added hidden:terrain:revealed event to GameEvent discriminated union (types.ts)
- Wired CuriositySystem into Game.ts (after ZoomiesSystem in the fixed-step loop)
- pnpm typecheck and pnpm lint both pass
- **Learnings for future iterations:**
  - HiddenTerrain revealCount pattern handles multiple overlapping Curiosity cats: terrain stays visible as long as any cat covers it
  - CuriositySystem needs SceneManager access (injected) to call setMeshOpacity() — this is the only system besides CatPlacementSystem that holds a SceneManager ref
  - Hidden terrain's Renderable is created normally by MapManager (opacity:0); RenderSystem registers it as a mesh. CuriositySystem then calls setMeshOpacity(handle, 1) using the sceneHandle stored by RenderSystem. This means reveal only works after the first RenderSystem frame — not a problem in practice since the cat is placed by the user.
  - Collider collisionMask is mutable on the component object; CuriositySystem toggles mask 0↔1 directly without destroying/recreating the entity
- For Next.js route handler tests (Vitest): mock `server-only`, `~/env`, `~/server/db`, and `@workos-inc/authkit-nextjs` via vi.mock before imports; use vi.mocked() for typed access to mock fns
- `import type { x } from "server-only-module"` (type-only imports) does NOT trigger the server-only guard — safe in shared helper type signatures
- navigator.sendBeacon test pattern: vi.stubGlobal("window", {addEventListener, removeEventListener}) + vi.stubGlobal("navigator", {sendBeacon: vi.fn()}); clean up with vi.unstubAllGlobals() in afterEach

---

## 2026-05-05 - US-201
- Extended GameState with path-based API: get<T>(path), set<T>(path, value), onChange<T>(path, callback), serialize(), restore()
- Added internal InternalState object with player/world/session sub-objects; session excluded from serialize()
- Preserved all existing methods (addYarn, deductYarn, onYarnChange, addResource, hasInventorySpace, onInventoryChange) as wrappers
- Added isDirty / clearDirty() for auto-save tracking
- Added validation in set() for player.yarn (>=0), player.stats.health (0..maxHealth), player.stats.level (0-10 integer), player.stats.maxHealth (>0)
- restore() snapshots old values before hydrating, then fires all registered path listeners with correct (newVal, oldVal)
- Files changed: src/game/engine/GameState.ts, src/game/__tests__/engine/GameState.test.ts
- All 167 tests pass; pnpm typecheck/lint/build all clean
- **Learnings for future iterations:**
  - TypeScript won't allow `this._state as Record<string,unknown>` directly — must go through `unknown` first: `as unknown as Record<string,unknown>`
  - addResource() must clone the inventory array before mutating (to give set() a new reference and properly trigger onChange callbacks)
  - onYarnChange/onInventoryChange emit immediately (existing contract) — the path-based onChange() does NOT emit immediately; wrappers call listener(currentVal) before registering
  - SaveData interface lives in GameState.ts for US-201; US-202 will formalize it in src/game/state/SaveData.ts with Zod validation
---

## 2026-05-05 - US-202
- Created src/game/state/SaveData.ts: saveDataSchema Zod schema + CURRENT_VERSION = '0.1'
  - Schema validates { character: { appearance, stats, inventory, position, yarn, oxygen, abilities }, world: { currentMapId, activeCats, resourceNodeCooldowns, hiddenTerrain }, session: { totalPlaytimeMs } }
  - Uses z.nativeEnum(CatType) and z.nativeEnum(ResourceType) for enum validation
  - Safe for import from both client game engine and server (no browser/server-only imports)
- Created src/game/state/migrations.ts: migrateIfNeeded(data: unknown): SaveData + SaveMigrationError
  - KNOWN_VERSIONS set guards against future/unknown versions (throws with user-facing message)
  - Handles both wrapped { version, saveData } and flat { version, ...content } input formats
  - Exported migration_0_1_to_0_2 stub showing the no-op pattern for future migrations
- Updated src/server/api/routers/game.ts: upsertSave.saveData now validated with saveDataSchema
- Updated src/game/engine/Game.ts: GameTrpcAdapter.upsertSave now typed with SaveData; removed
  the broken placeholder triggerAutoSave() (US-203 Persistence will handle auto-save properly)
- Tests: 21 new tests in src/game/__tests__/engine/SaveData.test.ts; 188 total pass
- All checks pass: pnpm test && pnpm lint && pnpm typecheck && pnpm build
- **Learnings for future iterations:**
  - Empty array literals like `inventory: []` in helper functions infer as `never[]` -- add `as Array<...>` type annotations OR use `const bad: unknown = {...}` for Zod invalid-value tests
  - migrateIfNeeded handles both { version, saveData: content } (tRPC getSave shape) and { version, ...content } (flat) by checking for raw["saveData"]
  - The active MIGRATIONS chain is empty at CURRENT_VERSION 0.1; migration_0_1_to_0_2 is exported but not in the chain yet -- add it when bumping to 0.2
  - GameTrpcAdapter in Game.ts was using Record<string, unknown> for saveData -- now uses SaveData from state/SaveData.ts; Game.ts placeholder auto-save was removed
---

## 2026-05-05 - US-204
- Created src/server/game/upsertGameSave.ts: shared Drizzle upsert helper (db, userId, version, saveData)
- Updated src/server/api/routers/game.ts: upsertSave mutation now delegates to upsertGameSave
- Created src/app/api/game/beacon-save/route.ts: POST handler with withAuth session check, Zod validation (beaconBodySchema), calls upsertGameSave; returns 401/400/500 on failure paths
- Updated src/game/state/Persistence.ts: added setupBeforeUnload() + _beforeUnloadHandler cleanup in dispose()
- 10 new tests: 5 beacon route (401/400-json/400-zod/200/500), 5 beforeunload (register/unregister/fires/graceful-fallback/no-remove-if-not-setup); 232 total pass
- All checks pass: pnpm test && pnpm lint && pnpm typecheck && pnpm build
- **Learnings for future iterations:**
  - For Next.js route handler tests: mock `server-only`, `~/env`, `~/server/db`, and auth provider; then import route under test — vi.mock hoisting ensures mocks are in place
  - `import type` from a `server-only` module is safe in shared helpers (type imports don't run module code)
  - Persistence._beforeUnloadHandler is null until setupBeforeUnload() is called; dispose() guards with null check so it's safe to call before setup
---

## 2026-05-05 - US-203
- Created src/game/state/Persistence.ts: save/load/auto-save orchestrator
- Key design: GameState uses internal { player, world } format; SaveData.ts Zod schema uses { character, world, session } — Persistence bridges via toExternalSaveData/fromExternalSaveData helpers (both exported for US-205)
- save(): serializes, validates with saveDataSchema, upserts via trpcAdapter, clears isDirty, emits save:failed on error (never crashes)
- load(): calls trpcAdapter.getSave(), runs migrateIfNeeded, returns ExternalSaveData | null (propagates errors for caller to handle)
- startAutoSave(intervalMs): setInterval; skips if !isDirty; position-only dirty is throttled 5s (tracked via _positionHasChangedSinceLastSave + _lastPositionDirtyAt)
- forceSave(): calls save() directly, bypasses auto-save dirty checks
- restoreFromSave(data): calls fromExternalSaveData then gameState.restore() — used by US-205 load flow
- dispose(): clears interval + all onChange subscriptions
- Extended GameTrpcAdapter with getSave(): Promise<{version, saveData} | null>; implemented in GameCanvas.tsx via useQuery refetch pattern (enabled:false, staleTime:Infinity)
- Added save:failed event to GameEvent discriminated union in types.ts
- 34 new tests in src/game/__tests__/engine/Persistence.test.ts (222 total pass)
- All checks pass: pnpm test && pnpm lint && pnpm typecheck && pnpm build
- **Learnings for future iterations:**
  - GameState.serialize() returns { player, world }; saveDataSchema expects { character, world, session } — the mapping is in Persistence.ts (toExternalSaveData/fromExternalSaveData), not in GameState
  - Position dirty throttle: subscribe to onChange('player.position') to set _lastPositionDirtyAt + _positionHasChangedSinceLastSave; non-position paths each subscribe to set _hasNonPositionDirty; auto-save checks both flags
  - GameCanvas.tsx getSave() uses useQuery with enabled:false + refetch — tRPC returns saveData as unknown so cast to Record<string,unknown> is needed
  - vi.useFakeTimers() mocks Date.now() too, so timestamp comparisons in throttle logic work correctly in tests; use vi.advanceTimersByTimeAsync() to run intervals + flush microtasks
---

## 2026-05-05 - US-205
- Integrated Persistence into Game.ts constructor (step 14 in init chain)
- Refactored start() into start() + startFresh() + private _boot(saveData|null)
  - start(): loads save (throws on error, caller catches), then calls _boot
  - startFresh(): bypasses load, calls _boot(null) — used by GameCanvas "Start New Game"
  - _boot(): restoreFromSave → loadMap → spawnNodes → applyCooldowns → spawnPickups → spawnPlayer → restoreCats → setupBeforeUnload → startAutoSave → render loop
- spawnPlayer() extended with optional positionOverride: Vec3 (used when restoring from save)
- spawnTestMapResourceNodes() now tracks nodeId→entity in _nodeIdMap ("node_x_z" format)
- _applyResourceNodeCooldowns(): applies saved cooldowns to ResourceNode components (no visual dim on restore — cosmetic limitation noted)
- _restoreActiveCats(): validates position via catManager.isValidPosition(), pre-adds yarn before summon then summon deducts it back (net 0 change)
- GameCanvas.tsx: added StartState (idle/loading/error/running), awaits game.start(), shows loading overlay, shows error overlay with [Retry] / [Start New Game] buttons
- 15 new integration tests in src/game/__tests__/integration/load-restore.test.ts: yarn invariant, entity creation, position skip, cooldown application, Persistence.load() pipeline
- All 247 tests pass; pnpm test && pnpm lint && pnpm typecheck && pnpm build all clean
- **Learnings for future iterations:**
  - Cat restore yarn trick: isValidPosition() check first, then addYarn(cost) + summon() (which deducts cost back), then deductYarn(cost) if summon returns null
  - position-based nodeId ("node_x_z") allows matching entities to save data without modifying ResourceNode component shape
  - _boot(null) is the correct "start fresh" path — avoids code duplication between start() and startFresh()
  - GatheringSystem handles resource node visual dimming only when gathering completes; restored cooldowns won't appear dim visually on first load (cosmetic only, gameplay is correct)
  - startFresh() is synchronous — no async needed since it bypasses Persistence.load()
---

## 2026-05-05 - US-206
- Created src/game/ui/DebugMenu.ts: tabbed debug overlay panel (dev builds only)
- Added `debug:value-changed` event to GameEvent discriminated union in types.ts
- Panel: rgba(0,0,0,0.85) centered overlay, high z-index, Ctrl+D toggle, does not pause game loop
- Tab bar: Player (active) | Cats | World | Session — last three show "coming in US-20X" placeholder
- Player tab controls: level (number 0-10), health (range 0-maxHealth), yarn (number 0-99), teleport (X/Z inputs + Go button), speed multiplier (range 0.5x-3.0x), god mode (checkbox)
- All Player-tab mutations route through GameState.set() or runtimeCfg.walkSpeed mutation
- applyTeleport() accepts optional teleportPlayer callback (passed from Game.ts) for proper physics teleport
- debugState { godMode, speedMultiplier } lives only on DebugMenu — never touches GameState.serialize()
- Game.ts: added DebugMenu as a non-readonly private field, wired in constructor (step 15, dev-only) with physics teleport closure; dispose() called in destroy()
- 27 new tests in src/game/__tests__/engine/DebugMenu.test.ts — DOM stubbed via vi.stubGlobal, NODE_ENV via vi.stubEnv
- All 274 tests pass; pnpm lint, typecheck, build all clean
- **Learnings for future iterations:**
  - `readonly` field assigned conditionally after `= null` initializer can't be re-assigned via a cast — remove `readonly` and assign directly in the `if` block
  - Tests stub document/window with vi.stubGlobal since node env has no DOM; vi.stubEnv('NODE_ENV', 'development') enables the dev-only constructor path in tests
  - DebugMenu exposes `apply*()` public methods so tests (and keyboard handlers alike) call the same logic — no duplication between DOM event handlers and direct test calls
  - debugState (godMode, speedMultiplier) stores debug-only overrides; runtimeConfig.walkSpeed is mutated directly (MovementSystem reads runtimeConfig, not CONFIG)
  - _baseWalkSpeed captured from CONFIG.walkSpeed at construction time so multiplier math is always relative to the canonical value
---

## 2026-05-05 - US-207
- Added Cats tab to DebugMenu, replacing the "Coming soon" placeholder
- Extended DebugMenu constructor: optional `catCompanionManager?: CatCompanionManager` (7th param) and `getPlayerPosition?: () => Vec3 | null` (8th param) — backward-compatible; existing tests unchanged
- New public methods: `applyForceSummon(catType)`, `applyYarnOverride(value)`, `applyCatLimitOverride(value)`, `applyDismissAll()`
- New public `update()` method: refreshes active-cat status display each frame (no-ops when menu is closed or catCompanionManager not wired)
- Force-summon: temporarily sets yarn +9999 and maxActiveCats=999, calls summon(), restores original values regardless of outcome
- Game.ts: passes `this.catCompanionManager` and a `getPlayerPosition` closure to DebugMenu; calls `debugMenu?.update()` in the variable-rate render pass
- Added `_divider()` helper to DebugMenu for visual separation between sections
- 16 new tests in DebugMenu.test.ts; 290 total pass
- All checks pass: pnpm test && pnpm lint && pnpm typecheck && pnpm build
- **Learnings for future iterations:**
  - Force-summon via boost+restore pattern: set yarn/limit high, call real summon(), restore — avoids duplicating summon() logic; yarn net change is always zero
  - `vi.fn()` without a return-type annotation produces `Mock<Procedure | Constructable>` which TypeScript won't unify with a typed function signature; fix with `as unknown as () => T` at the call site
  - `update()` is a no-op guard pattern: return early if `!_visible || !_catStatusContainer || !catCompanionManager` — safe to call every frame
  - Optional constructor params added after existing ones preserve backward-compatibility; no existing tests needed updating
---

## 2026-05-05 - US-208
- Added World tab to DebugMenu: time scale slider (0.25x–4.0x), wireframe toggle, reload map button, entity count display, Fill/Empty all resource nodes, spawn resource node (type + X/Z inputs)
- Added `timeScale: number = 1.0` to GameConfig (config.ts) and BASE_CONFIG
- Game.ts fixed-step loop now computes `scaledDt = FIXED_DT * runtimeConfig.timeScale` and passes it to all systems; InputManager.poll() is outside the loop and unaffected
- Added `toggleWireframes(enabled: boolean)` to SceneManager: iterates registered meshes and sets material.wireframe
- Added `get entityCount(): number` to World (exposes this.entities.size)
- DebugMenu constructor extended with optional `reloadMap?: () => void` and `sceneManager?: { toggleWireframes }` (params 9 and 10)
- `update()` now also refreshes `_entityCountEl` (total + cats + nodes breakdown) each frame when menu is open
- New apply methods: applyTimeScale, applyReloadMap, applyWireframes, applyFillAllNodes, applyEmptyAllNodes, applySpawnResourceNode
- applySpawnResourceNode: creates entity in world with Transform + Renderable + Collider + ResourceNode; RenderSystem picks it up on next frame
- 21 new tests; 311 total pass; pnpm test && lint && typecheck && build all clean
- **Learnings for future iterations:**
  - timeScale scales FIXED_DT passed to systems inside the accumulator loop; the accumulator drains by real FIXED_DT so step rate stays stable at 60Hz
  - SceneManager.toggleWireframes iterates all registered meshes — includes cat/resource/terrain meshes; debug-only so visual purity is acceptable
  - World.entityCount is a simple `this.entities.size` getter; no cache invalidation needed
  - applySpawnResourceNode creates entities directly in World; no SceneManager call needed — RenderSystem auto-registers new Renderable entities
  - MapManagerLike / SceneManagerRef minimal interfaces defined in DebugMenu.ts to avoid importing full engine types into the UI layer
---

## 2026-05-05 - US-209
- Added Session tab to DebugMenu: Force Save, Force Load, Reset All State, Last Saved timestamp, FPS counter, session elapsed time, auto-save toggle
- Extended GameTrpcAdapter interface with `deleteSave(): Promise<void>`; wired via `api.game.deleteSave.useMutation()` in GameCanvas.tsx
- Added `Persistence.deleteSave()` delegating to `trpcAdapter.deleteSave()`
- DebugMenu constructor gains params 11 (`persistence?: PersistenceLike`) and 12 (`onSaveReset?: () => void`)
- `update()` signature changed to `update(dt?: number)` — stores `_sessionLastDt` for per-frame FPS display; Game.ts now passes `realDt`
- `applyResetAllState()` calls `window.confirm()`, then `persistence.deleteSave()`, then `onSaveReset()` (which triggers `window.location.reload()` in Game.ts)
- 14 new tests; 325 total pass; all checks clean
- Updated existing `makeMockAdapter` in Persistence.test.ts and 4 inline adapters in load-restore.test.ts to include `deleteSave` mock
- **Learnings for future iterations:**
  - `PersistenceLike` is a module-private interface in DebugMenu.ts; tests cast mocks as `unknown as Persistence` (structural match is sufficient)
  - Inner `afterEach` must NOT call `vi.unstubAllGlobals()` if outer `afterEach` still needs `document` — let the outermost cleanup handle global stubs
  - `vi.fn()` produces `Mock<Procedure | Constructable>` — cast with `as unknown as () => void` when passing as a typed callback parameter
  - `window.confirm` requires `vi.stubGlobal("window", { confirm: vi.fn(() => true) })` in node test env; main afterEach's `vi.unstubAllGlobals()` handles cleanup
---
