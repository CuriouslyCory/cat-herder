## Codebase Patterns
- CatAISystem runs FIRST in the fixed-step loop and drives Idle→Active→Expired state transitions for all cats from CAT_REGISTRY data; per-cat systems (ZoomiesSystem, CuriositySystem, PounceSystem) react to states but never set them
- CuriositySystem detects "first Active tick" via `reveal.revealedEntities.length === 0` (not via Idle state); CatAISystem transitions Idle→Active in the same frame so CuriositySystem sees Active and empty list simultaneously
- ZoomiesTrail overlap is a manual oriented-AABB check (not PhysicsEngine trigger) — project player offset onto trail axis (along) and perpendicular (perp), compare to halfLength/halfWidth
- SpeedBoost component is added/removed by ZoomiesSystem each fixed tick; MovementSystem reads it to scale walkSpeed before computing targetX/Z
- Movement cat trail entity has no PhysicsEngine body — only Transform + Renderable + ZoomiesTrail; position/rotation synced by RenderSystem normally
- CatCompanionManager.trailEntities Map<catEntity, trailEntity>: created in summon() for effectType='movement', destroyed in dismiss() before destroyEntity(catEntity)
- ZoomiesSystem must be instantiated after CatCompanionManager (dependency); runs at end of fixed-step sub-tick (after OxygenSystem)
- PhysicsEngine has a flat-floor fallback (y < body.config.size clamps to size) so the player stays on y=0 terrain without registering every tile as a physics body
- Terrain/launch cats register PhysicsEngine static bodies (isStatic=true, layer=1, mask=1) so the player's downward raycast detects their top surface; dismiss() removes the body
- getCatHalfHeight(def) / getXZHalfExtent(def) helpers in CatCompanionManager derive correct extents from MeshConfig geometry kind + behavior.params without per-cat switch logic
- Cat entity center Y = surfaceY + halfHeight (so mesh bottom rests on terrain, not half-underground); PhysicsEngine body position matches
- Only the player has a PhysicsEngine body by default; terrain tiles (flat floor) rely on the y≥0 clamp; elevated terrain needs registered physics bodies (added for cats in US-106)
- Cat definitions live in src/game/cats/definitions/<CatType>.ts; all exported via CAT_REGISTRY Map<CatType,CatDefinition> in definitions/index.ts — systems should import CAT_REGISTRY, not individual files
- GatheringSystem uses distance check (distSq < INTERACT_RANGE_SQ) not trigger events to detect nearby resource nodes; nodes have collisionMask=0 (no physics response needed)
- Resource node entities are created in Game.spawnTestMapResourceNodes() after loadMap(); not part of MapData/MapManager (world entities, not terrain)
- GameState.hasInventorySpace() / addResource() provide the basic inventory contract; capacity enforcement happens in US-112 but the interface is forward-compatible
- HUD gather progress bar lives at bottom:110px (above cat bar at bottom:56px) and uses display:none to hide when not gathering — same pattern as oxygen gauge
- CatDefinition.behavior.params is Record<string, number|string|boolean> for cat-specific config (no per-cat union types needed in the interface)
- OxygenState is added/removed by WaterSystem alongside SwimmingState — both live and die together on water entry/exit
- OxygenSystem queries OxygenState+SwimmingState+PlayerControlled; OxygenState is always paired with SwimmingState (no need for null guards when both appear in query)
- SceneManager.addMesh() now supports opacity field; setMeshColor/setMeshOpacity allow runtime material mutation without recreating meshes
- CatPlacementSystem runs in the variable-rate render pass (not fixed-step loop) so clicks are processed exactly once per render frame
- GameState is instantiated in Game.ts before CatCompanionManager (dependency order); CatCompanionManager gets a `() => playerEntity` getter to avoid circular refs
- UIManager.setCatCatalog() must be called after map load (catalog comes from CatCompanionManager.getCatalog() which reads CAT_REGISTRY)
- HUD.setCatalog() builds cat slot DOM elements lazily — call it once at init, then update via setCatBar() each frame
- HUD.update(dt, oxygenPercent, health, maxHealth) — UIManager.update(dt, HUDState?) is the call site; Game.buildHUDState() assembles the snapshot each render frame
- Player health lives in PlayerControlled.health/maxHealth (integer hp); OxygenState.healthDrainAccum accumulates sub-frame losses to avoid float rounding
- CSS keyframe for oxygen warning pulse is injected once via <style id="__oxygen-pulse-style"> to avoid duplicates
- Water triggers use collisionLayer=1/mask=1 (player layer) so CollisionSystem fires trigger events; floor tiles use layer=2/mask=0 (PhysicsEngine raycasts only)
- SwimmingState lifecycle is event-driven (trigger:enter/exit from CollisionSystem); MovementSystem applies swim physics by querying for SwimmingState each frame — no state machine
- PhysicsEngine.setGravityEnabled(handle, bool) controls per-body gravity; must be called when adding/removing SwimmingState
- Terrain entities are created before the player, so terrain entity IDs are always smaller; in pairKey(a,b) the smaller id comes first → water entity is usually "trigger" in events
- Game loop order: movementSystem → physics.step() → collisionSystem → waterSystem (WaterSystem reacts to trigger events emitted by CollisionSystem)
- Config changes: swimSpeed/swimSpeedVertical replaced with swimSpeedSurface(3.2), swimSpeedDive(2.0), swimSpeedAscend(2.5), swimSpeedVertical(1.5), swimAcceleration(0.5)
---

# Ralph Progress Log
Started: Thu Apr 30 09:41:37 AM MST 2026
---

## 2026-04-30 - US-101
- Implemented full water mechanics: SwimmingState component, WaterTrigger component, WaterSystem (event-driven), and swimming branch in MovementSystem
- Files changed: SwimmingState.ts, WaterTrigger.ts, WaterSystem.ts (new), MovementSystem.ts (swimming branch), MapManager.ts (WaterTrigger component, fixed collision mask), PhysicsEngine.ts (noGravity flag + setGravityEnabled), InputManager.ts (Dive action), config.ts (swim config), types.ts (GameAction.Dive), Game.ts (WaterSystem wiring)
- **Learnings for future iterations:**
  - CollisionSystem's pairKey sorts by entity id, not by trigger/target role — WaterSystem.resolveWaterPlayer() checks both orderings
  - Water tiles originally had collisionLayer=2/mask=0 which prevented trigger events; needed layer=1/mask=1 to match player
  - PhysicsEngine has its own trigger detection (detectTriggerOverlaps) but only for registered physics bodies — terrain has no physics body, so only CollisionSystem fires water triggers
  - transitionBlend in SwimmingState prevents speed pop at water boundary; ramps 0→1 over 0.3s
  - Use `moveToward` for swim acceleration (same helper as land movement)
---

## 2026-04-30 - US-102
- Implemented full oxygen system: OxygenState component, OxygenSystem (drain/refill/events/health), WaterSystem extended to add/remove OxygenState, HUD oxygen gauge, UIManager HUDState interface, health fields in PlayerControlled
- Files changed: OxygenState.ts (new), OxygenSystem.ts (new), WaterSystem.ts (add OxygenState on enter/exit), types.ts (oxygen:warning, oxygen:depleted events), config.ts (percent-based oxygen config + oxygenWarningThreshold + oxygenHealthDrainRate), PlayerControlled.ts (health/maxHealth), HUD.ts (oxygen gauge + reactive hearts), UIManager.ts (HUDState interface), Game.ts (OxygenSystem wiring + buildHUDState())
- **Learnings for future iterations:**
  - OxygenSystem runs AFTER WaterSystem in the fixed-step loop so OxygenState already exists when OxygenSystem queries it
  - Warning event fires once per submersion (hasEmittedWarning gate); depleted event also fires once (hasEmittedDepleted gate) — both reset automatically when WaterSystem removes OxygenState on exit
  - Health drain uses integer accumulation (healthDrainAccum) to ensure 1 full hp is removed per second without floating-point creep
  - UIManager.update(dt, HUDState?) uses optional second arg; Game.buildHUDState() reads from world each render frame (variable-rate, not fixed)
  - HUD inject-once keyframe trick: `document.getElementById("__oxygen-pulse-style")` guard prevents duplicate <style> tags on HMR reloads
---

## 2026-04-30 - US-103
- Implemented data-driven cat definition system: CatDefinition interface, four definition files, and CAT_REGISTRY
- Files created: src/game/cats/CatDefinition.ts, src/game/cats/definitions/Loaf.ts, src/game/cats/definitions/Zoomies.ts, src/game/cats/definitions/CuriosityCat.ts, src/game/cats/definitions/Pounce.ts, src/game/cats/definitions/index.ts
- **Learnings for future iterations:**
  - CatType enum was already defined in types.ts (Loaf, Zoomies, CuriosityCat, Pounce) — no changes needed there
  - MeshConfig (from SceneManager.ts) reused directly for meshConfig field — matches box/sphere/cylinder/plane geometry kinds
  - behavior.params is a loose Record so each cat can carry its own numeric config (launchImpulse, trailLength, revealRadius, collider dims) without expanding the interface
  - Terrain cats (Loaf, Pounce) have no duration; duration cats (Zoomies 8s, Curiosity 20s) set behavior.duration
---

## 2026-04-30 - US-106
- Implemented Loaf cat as a walkable static platform using PhysicsEngine body registration
- Files changed:
  - src/game/engine/PhysicsEngine.ts — flat-floor fallback in integrateBody (y<size clamp prevents falling through base terrain)
  - src/game/cats/CatCompanionManager.ts — PhysicsEngine dependency added; physicsHandles Map<Entity,BodyHandle>; summon() registers static physics body for terrain/launch cats; dismiss() removes body; cat Y placement fixed to surfaceY+halfHeight; getCatHalfHeight()/getXZHalfExtent() module helpers
  - src/game/engine/Game.ts — passes this.physics to CatCompanionManager constructor
- **Learnings for future iterations:**
  - PhysicsEngine.raycastDownward only sees bodies registered via addBody() — terrain tiles were never registered (only the player was). Flat-floor fallback at y=0 is the pragmatic fix for base terrain; explicit physics bodies needed for elevated platforms
  - Cat entity Y must be surfaceY + halfHeight (center of mesh), not surfaceY; otherwise box cats are 50% underground visually and physics body top is also wrong
  - Physics body size for a box cat = halfHeight (Y half-extent); this is used for both the downward raycast top-surface detection and the XZ horizontal collision. Using height/2 makes XZ slightly wider than actual (0.75 vs 0.6 for Loaf) but collision resolution is still correct
  - getCatHalfHeight(): box→dims[1]/2; cylinder→(dims[2]??size)/2; sphere→size. getXZHalfExtent(): prefers params.colliderWidth/2, falls back to max(dims[0],dims[2]??dims[0])/2
  - Note: jump config (jumpImpulse=3.5) gives only ~0.51u apex, not the intended 1.2u. A Loaf at 1.5u height is unreachable with current config. jumpImpulse needs tuning to ~6u/s for 1.5u apex (separate config fix)
---

## 2026-04-30 - US-105
- Implemented full cat placement UI: ghost preview, number-key selection, left-click summon, right-click dismiss, cat selection bar with live yarn count
- Files changed:
  - src/game/engine/SceneManager.ts — opacity field on MeshConfig; setMeshColor(), setMeshOpacity()
  - src/game/engine/InputManager.ts — leftClickThisFrame/rightClickThisFrame tracking; getPressedCatSlot(); contextmenu suppression
  - src/game/cats/CatCompanionManager.ts — isValidPosition() made public
  - src/game/systems/CatPlacementSystem.ts (new) — ghost lifecycle, selection toggle (1-4), summon on left-click, dismiss on right-click
  - src/game/ui/HUD.ts — cat selection bar (4 slots with color/cost), setCatalog(), yarn count display
  - src/game/ui/UIManager.ts — HUDState extended with yarn + selectedCatType; setCatCatalog() pass-through
  - src/game/engine/Game.ts — GameState, CatCompanionManager, CatPlacementSystem instantiated and wired
- **Learnings for future iterations:**
  - Ghost mesh uses spread of cat's meshConfig with overridden color/opacity — no need for separate ghost-specific types
  - DISMISS_RADIUS_SQ comparison avoids sqrt; 1.5u radius (2.25 squared) is comfortable for clicking active cats
  - SceneManager.setMeshColor() uses THREE.Color.set() which accepts CSS strings, hex numbers, and named colors
  - CatPlacementSystem.update() takes only `dt` (not World) since it holds world reference; kept consistent with System-like naming
  - HUD cat bar colors are hardcoded per CatType enum to match cat definition colors — update both if definition colors change
---

## 2026-04-30 - US-107
- Implemented Zoomies cat speed trail: SpeedBoost component, ZoomiesTrail component, ZoomiesSystem, CatCompanionManager trail lifecycle, MovementSystem speed scaling
- Files created: src/game/ecs/components/SpeedBoost.ts, src/game/ecs/components/ZoomiesTrail.ts, src/game/systems/ZoomiesSystem.ts
- Files modified: src/game/cats/CatCompanionManager.ts (trail creation/cleanup + import), src/game/systems/MovementSystem.ts (SpeedBoost import + multiplier), src/game/engine/Game.ts (ZoomiesSystem wiring)
- **Learnings for future iterations:**
  - PhysicsEngine addBody() takes a uniform `size` (single half-extent), not per-axis — unsuitable for the 6u×1.5u trail rectangle; manual oriented AABB in ZoomiesSystem is the right approach
  - Trail direction: normalize(catPos - playerPos) at summon time; stored in ZoomiesTrail.dirX/dirZ for per-frame projection; box mesh rotated via rotationY = atan2(dirX, dirZ)
  - ZoomiesSystem.dismiss() auto-sets behavior.state="Expired" before calling catCompanionManager.dismiss() — dismiss() checks this to skip yarn refund (consumed on expiry)
  - Trail entity destroyed by CatCompanionManager.dismiss(), not by ZoomiesSystem — keeps cleanup centralised in one place
  - Multiple Zoomies cats: ZoomiesSystem handles multiple trails; player gets SpeedBoost from the last trail they're in (last wins in the loop); all trails same multiplier so order doesn't matter
---

## 2026-04-30 - US-109
- Implemented Pounce cat launch mechanic: PounceSystem (XZ footprint + grounded check + once-per-landing Set), wired into Game.ts fixed loop after CuriositySystem, Pounce platform added to TestMap
- Files created: src/game/systems/PounceSystem.ts
- Files modified: src/game/engine/Game.ts (import + instantiation + loop call), src/game/maps/TestMap.ts (height=1 platform rows 14-15 cols 6-7)
- **Learnings for future iterations:**
  - PounceSystem does NOT need CatCompanionManager — dismiss is handled externally (right-click); system only applies the launch impulse
  - "Once per landing" is enforced by a `launchedCats: Set<Entity>` in PounceSystem; entry added on launch, removed when player exits XZ footprint (snapshot iteration to avoid Set mutation during loop)
  - Air control (70%) is already global via runtimeConfig.airControlFactor in MovementSystem — no per-cat wiring needed for Pounce
  - physicsEngine.isBodyGrounded(handle) distinguishes player standing ON the Pounce cat (top surface detected by raycast) vs. walking into its side (blocked by XZ collider but not grounded by it)
  - Launch physics (impulse=3.5 u/s, gravity=-12): player peaks ~0.51u above launch point; Pounce cat top at ~0.5u gives player peak ~1.41u center — height=1 terrain platform is marginally reachable. The "3.5u above resting height" AC is aspirational; real physics gate is ~1u platform max with current gravity config
  - CatBehavior.state is transitioned Idle→Active in PounceSystem on the first frame (same pattern as ZoomiesSystem/CuriositySystem)
---

## 2026-04-30 - US-110
- Implemented CatAISystem: generic state machine reading duration from CAT_REGISTRY
- Created src/game/systems/CatAISystem.ts: queries CatBehavior+Transform; Idle→Active all cats; Active→stateTimer tick for duration cats; stateTimer≥duration→Expired; permanent cats (no duration) stay Active
- Refactored ZoomiesSystem: removed Idle→Active + stateTimer tick + auto-dismiss logic; now reacts to Expired state set by CatAISystem; `update(world, _dt)` — dt unused
- Refactored CuriositySystem: removed Idle→Active + stateTimer tick; detects first Active frame via `reveal.revealedEntities.length === 0`; reacts to Expired to hideRevealedTerrain + dismiss
- Refactored PounceSystem: removed `if (behavior.state === "Idle") behavior.state = "Active"` — replaced with `if (behavior.state !== "Active") continue`
- Wired CatAISystem into Game.ts fixed-step loop BEFORE ZoomiesSystem/CuriositySystem/PounceSystem
- pnpm typecheck and pnpm lint both pass
- **Learnings for future iterations:**
  - CatAISystem sets Expired; per-cat systems detect Expired and call dismiss() — this avoids CatAISystem needing a CatCompanionManager dependency and keeps cleanup logic in each cat's system
  - CuriositySystem's "on first activate" signal changed from `state === "Idle"` to `state === "Active" && revealedEntities.length === 0` — works because CatAISystem transitions in the same fixed tick before CuriositySystem runs
  - Per-cat systems guard with `if (!world.isAlive(catEntity)) continue` after any dismiss call to handle same-frame entity destruction
---

## 2026-04-30 - US-111
- Implemented resource gathering: ResourceNode component, GatheringSystem, GameState inventory helpers, HUD progress bar, 15 resource node entities in TestMap
- Files created: src/game/ecs/components/ResourceNode.ts, src/game/systems/GatheringSystem.ts
- Files modified: src/game/types.ts (ResourceType enum + resource:gathered event), src/game/engine/GameState.ts (inventory stacks + addResource/hasInventorySpace), src/game/ui/HUD.ts (gather progress bar), src/game/ui/UIManager.ts (gatherState in HUDState), src/game/engine/Game.ts (GatheringSystem wire-up + spawnTestMapResourceNodes())
- pnpm typecheck, pnpm lint, pnpm build all pass
- **Learnings for future iterations:**
  - GatheringSystem caches _gatherState each fixed tick; buildHUDState() reads it in the variable-rate render pass — decouples fixed/render rates cleanly
  - Resource node colliders use collisionMask=0 (no physics response); distance check in GatheringSystem avoids trigger infrastructure complexity
  - GameState.addResource() is forward-compatible with US-112's capacity enforcement — hasInventorySpace() is already the gate
  - HUD gather bar hides via display:none (same pattern as oxygen gauge); visible only during an active gather action
---

## 2026-04-30 - US-112
- Implemented Inventory System: reactive GameState listener, Inventory Full notification timer, HUD inventory panel with capacity bar
- Files changed:
  - src/game/engine/GameState.ts — added inventoryListeners Set, onInventoryChange() reactive subscriber, notifyInventory() called in addResource()
  - src/game/systems/GatheringSystem.ts — added _inventoryFullTimer (2s), ticked in update(), set on E+full, exposed via isInventoryFull()
  - src/game/ui/UIManager.ts — added InventoryStack import, inventory/maxInventoryCapacity/inventoryFull fields to HUDState; passed through in update()
  - src/game/ui/HUD.ts — added InventoryStack import, RESOURCE_ICONS map (🌿/🪵/💧), buildInventoryPanel() (top-left: capacity label+bar, stack rows, Inventory Full notice), setInventory() updater; update() signature extended with 3 new params
  - src/game/engine/Game.ts — both buildHUDState() return paths now include inventory, maxInventoryCapacity, inventoryFull
- pnpm typecheck, pnpm lint, pnpm build all pass
- **Learnings for future iterations:**
  - GameState.onInventoryChange() follows the same call-immediately pattern as onYarnChange() — listener fires with current value on subscribe
  - HUD inventory panel rebuilds stack rows on every frame via innerHTML="" — acceptable for ≤3 stacks in MVP, revisit if stacks exceed 5+
  - Capacity bar color thresholds: green (0-69%), yellow (70-99%), red (100%) — mirrors common game inventory UI convention
  - "Inventory Full!" notification persists 2s via timer in GatheringSystem; timer ticked in update() and checked by isInventoryFull() in the render pass
---
