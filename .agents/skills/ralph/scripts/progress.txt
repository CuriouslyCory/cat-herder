## Codebase Patterns
- MapEditor pattern: same dev-gate as DebugMenu (constructor returns early in production, banner ref null). Game.ts stores as `mapEditor: MapEditor | null`, constructs with `if (process.env.NODE_ENV !== "production")` guard, calls `mapEditor?.dispose()` in destroy(). Takes GameLifecycle interface { pause, resume } as callbacks to avoid circular imports.
- Test self-referencing types: avoid `ReturnType<typeof fn>` in return expressions of the same fn — creates circular type inference error (TS7023). Define a named interface instead.
- CatAISystem runs FIRST in the fixed-step loop and drives Idle→Active→Expired state transitions for all cats from CAT_REGISTRY data; per-cat systems (ZoomiesSystem, CuriositySystem, PounceSystem) react to states but never set them
- CuriositySystem detects "first Active tick" via `reveal.revealedEntities.length === 0` (not via Idle state); CatAISystem transitions Idle→Active in the same frame so CuriositySystem sees Active and empty list simultaneously
- ZoomiesTrail overlap is a manual oriented-AABB check (not PhysicsEngine trigger) — project player offset onto trail axis (along) and perpendicular (perp), compare to halfLength/halfWidth
- SpeedBoost component is added/removed by ZoomiesSystem each fixed tick; MovementSystem reads it to scale walkSpeed before computing targetX/Z
- Movement cat trail entity has no PhysicsEngine body — only Transform + Renderable + ZoomiesTrail; position/rotation synced by RenderSystem normally
- CatCompanionManager.trailEntities Map<catEntity, trailEntity>: created in summon() for effectType='movement', destroyed in dismiss() before destroyEntity(catEntity)
- ZoomiesSystem must be instantiated after CatCompanionManager (dependency); runs at end of fixed-step sub-tick (after OxygenSystem)
- PhysicsEngine has a flat-floor fallback (y < body.config.size clamps to size) so the player stays on y=0 terrain without registering every tile as a physics body
- Terrain/launch cats register PhysicsEngine static bodies (isStatic=true, layer=1, mask=1) so the player's downward raycast detects their top surface; dismiss() removes the body
- getCatHalfHeight(def) / getXZHalfExtent(def) helpers in CatCompanionManager derive correct extents from MeshConfig geometry kind + behavior.params without per-cat switch logic
- Cat entity center Y = surfaceY + halfHeight (so mesh bottom rests on terrain, not half-underground); PhysicsEngine body position matches
- Only the player has a PhysicsEngine body by default; terrain tiles (flat floor) rely on the y≥0 clamp; elevated terrain needs registered physics bodies (added for cats in US-106)
- Cat definitions live in src/game/cats/definitions/<CatType>.ts; all exported via CAT_REGISTRY Map<CatType,CatDefinition> in definitions/index.ts — systems should import CAT_REGISTRY, not individual files
- GatheringSystem uses distance check (distSq < INTERACT_RANGE_SQ) not trigger events to detect nearby resource nodes; nodes have collisionMask=0 (no physics response needed)
- Resource node entities are created in Game.spawnTestMapResourceNodes() after loadMap(); not part of MapData/MapManager (world entities, not terrain)
- GameState.hasInventorySpace() / addResource() provide the basic inventory contract; capacity enforcement happens in US-112 but the interface is forward-compatible
- HUD gather progress bar lives at bottom:110px (above cat bar at bottom:56px) and uses display:none to hide when not gathering — same pattern as oxygen gauge
- CatDefinition.behavior.params is Record<string, number|string|boolean> for cat-specific config (no per-cat union types needed in the interface)
- OxygenState is added/removed by WaterSystem alongside SwimmingState — both live and die together on water entry/exit
- OxygenSystem queries OxygenState+SwimmingState+PlayerControlled; OxygenState is always paired with SwimmingState (no need for null guards when both appear in query)
- SceneManager.addMesh() now supports opacity field; setMeshColor/setMeshOpacity allow runtime material mutation without recreating meshes
- CatPlacementSystem runs in the variable-rate render pass (not fixed-step loop) so clicks are processed exactly once per render frame
- GameState is instantiated in Game.ts before CatCompanionManager (dependency order); CatCompanionManager gets a `() => playerEntity` getter to avoid circular refs
- UIManager.setCatCatalog() must be called after map load (catalog comes from CatCompanionManager.getCatalog() which reads CAT_REGISTRY)
- HUD.setCatalog() builds cat slot DOM elements lazily — call it once at init, then update via setCatBar() each frame
- HUD.update(dt, oxygenPercent, health, maxHealth) — UIManager.update(dt, HUDState?) is the call site; Game.buildHUDState() assembles the snapshot each render frame
- Player health lives in PlayerControlled.health/maxHealth (integer hp); OxygenState.healthDrainAccum accumulates sub-frame losses to avoid float rounding
- CSS keyframe for oxygen warning pulse is injected once via <style id="__oxygen-pulse-style"> to avoid duplicates
- Water triggers use collisionLayer=1/mask=1 (player layer) so CollisionSystem fires trigger events; floor tiles use layer=2/mask=0 (PhysicsEngine raycasts only)
- SwimmingState lifecycle is event-driven (trigger:enter/exit from CollisionSystem); MovementSystem applies swim physics by querying for SwimmingState each frame — no state machine
- PhysicsEngine.setGravityEnabled(handle, bool) controls per-body gravity; must be called when adding/removing SwimmingState
- Terrain entities are created before the player, so terrain entity IDs are always smaller; in pairKey(a,b) the smaller id comes first → water entity is usually "trigger" in events
- Game loop order: movementSystem → physics.step() → collisionSystem → waterSystem (WaterSystem reacts to trigger events emitted by CollisionSystem)
- Config changes: swimSpeed/swimSpeedVertical replaced with swimSpeedSurface(3.2), swimSpeedDive(2.0), swimSpeedAscend(2.5), swimSpeedVertical(1.5), swimAcceleration(0.5)
- DebugMenu pattern: gated by `process.env.NODE_ENV !== "production"` at construction, returns early leaving all element refs null. DOM panel built via imperative createElement, appended to canvas parent. Ctrl+D toggles. Game.ts stores as `debugMenu: DebugMenu | null` and calls debugMenu?.update(realDt) each frame + debugMenu?.dispose() in destroy().
- Persistence: auto-save interval + beforeunload beacon. Persistence.forceSave()/load()/deleteSave() for debug. SaveData versioned with migrations in src/game/state/migrations.ts.
- MapData interface: { name, size:{width,depth}, terrain:TerrainCell[][], cellSize, spawnPoints:SpawnPoint[] }. MapManager.loadMap() builds entities from terrain grid via greedy-rectangle merge.
- CameraController supports CameraMode = "follow" | "free"; free mode allows panBy(dx,dz) and setFocus(x,y,z). Follow mode tracks targetEntity.
---

# Ralph Progress Log — Phase 4: Map Editor & Polish
Started: 2026-05-08
---

## 2026-05-08 - US-301
- Created src/game/maps/MapEditor.ts with full public API: enable(), disable(), isActive(), getMapData(), loadMapData(data)
- Ctrl+E keyboard handler toggles editor; enter pauses game + setMode('free'), exit resumes + setMode('follow')
- EDITOR MODE banner rendered as DOM overlay (top-center, z-index:9998), shown/hidden via display:block/none
- Production-gated: constructor returns early when NODE_ENV=production, banner stays null, enable() is a no-op
- Wired into Game.ts: imported MapEditor, added `private mapEditor: MapEditor | null`, constructed after DebugMenu with same dev-gate pattern, disposed in destroy()
- Created src/game/__tests__/engine/MapEditor.test.ts with 27 tests covering all acceptance criteria
- Files changed: src/game/maps/MapEditor.ts (new), src/game/__tests__/engine/MapEditor.test.ts (new), src/game/engine/Game.ts (modified)
- **Learnings for future iterations:**
  - GameLifecycle interface { pause, resume } passed as constructor param avoids circular import between maps/ and engine/
  - CameraController.setMode('follow') preserves the existing targetEntity (follow restores to last-followed entity)
  - document.body is used as fallback when container.parentElement is null/undefined (same as DebugMenu)
  - Self-referencing types in mock helpers cause TS7023; define named interface instead of ReturnType<typeof fn>
---

## 2026-06-29 - US-302a
- Verified terrain tool palette & placement implementation in src/game/maps/MapEditor.ts.
  Side panel (left, 160px) built in _buildPanel() with a "Terrain" section; _buildToolButton()
  renders one button per PLACEABLE_TOOLS type (Grass/Dirt/Stone/Water), each with a color swatch
  (EDITOR_TERRAIN_COLORS, kept in sync with MapManager TERRAIN_COLORS) + type-name label.
- selectTool()/getSelectedTool() manage tool state; clicking a tool toggles selection and
  highlights the active button. Panel shown (display:flex) on enable(), hidden on disable().
- placeBlock(worldX, worldZ) snaps via static snapToGrid() = Math.round(); blocks stored in
  internal _editorBlocks[] (default height 1u). Re-placing on an occupied cell updates type.
- Ghost preview (_updateGhost) is a semi-transparent (opacity 0.4) box that follows the cursor
  at the snapped position via the mousemove handler; only present while a terrain tool is selected,
  removed on deselect/disable.
- Quality gate all green: pnpm test (694 passing), pnpm lint, pnpm typecheck, pnpm build.
- Files: src/game/maps/MapEditor.ts, src/game/__tests__/engine/MapEditor.test.ts (both pre-existing
  on this branch; US-302a was already committed as d410bb4). Marked passes=true in scripts/prd.json.
- **Learnings for future iterations:**
  - EDITOR_TERRAIN_COLORS in MapEditor.ts must stay in sync with TERRAIN_COLORS in MapManager.ts
    (no shared constant — duplicated to avoid the maps/→engine/ import; update both together).
  - SceneManager is an optional ctor arg (null in unit tests); placeBlock/ghost guard on it so
    state logic is testable without Three.js. Tests pass a MockSceneMgr (returns Symbol handles)
    when verifying addMesh/updateTransform/opacity calls.
  - Ghost/placement mouse handlers exclude the ghost handle from screenToWorld() raycasts so the
    ghost mesh doesn't occlude the ground pick.
---

## 2026-06-29 - US-303
- Verified & finalized entity placement in MapEditor: Player Spawn (single, marker #ff6b35), Cat Spawn (multiple, purple #9b59b6), Resource Node (Grass/Sticks/Water dropdown + respawn config), Hidden Terrain Zone (click-drag rect + height), Yarn Pickup (amount, default 3)
- Player spawn uniqueness enforced in _placePlayerSpawn(): existing spawn mesh removed before placing the new one
- Resource respawn defaults exported as RESOURCE_RESPAWN_DEFAULTS (Grass 30s / Sticks 45s / Water 60s); switching resource type in the side panel resets respawn input to the type default
- Hidden terrain uses the same drag-to-define flow as water zones (mousedown start → mousemove ghost → mouseup createHiddenTerrainZone)
- All entity markers are distinct colored meshes via _createEntityMarkerMesh / ENTITY_COLORS + RESOURCE_NODE_COLORS
- Files: src/game/maps/MapEditor.ts, src/game/__tests__/engine/MapEditor.test.ts (entity tests covering spawn uniqueness, config defaults, drag zones, dispose cleanup)
- Quality gate: pnpm test (694 pass) && pnpm lint && pnpm typecheck && pnpm build all green
- **Learnings for future iterations:**
  - `pnpm typecheck` standalone can fail with `.next/types/validator.ts ... Cannot find module './routes.js'` if a build hasn't been run recently — that file is generated by `pnpm build`. Run build first, then typecheck passes clean. This is a Next.js generated-types staleness issue, not a source error.
  - Entity tool selection and terrain tool selection are mutually exclusive: selectEntityTool() clears _selectedTool (and vice versa) plus any editor tool mode, so the click handler routes to exactly one placement path.
  - Two PRD copies exist: .agents/skills/ralph/scripts/prd.json is the active one; the old root .agents/skills/ralph/prd.json (different US-301..US-324 set) is deleted in the working tree.
---

## 2026-06-29 - Orchestrator verification sweep (US-302a → US-312)
- DISCOVERY: All stories US-301 through US-312 were already implemented and committed on
  feat/map-editor-and-polish-v2 (see git log: feat: US-302a ... feat: US-311b). The
  scripts/prd.json passes flags had been reset to false during the v2 PRD copy and were
  out of sync with the actual codebase. No re-implementation was needed — fanning out
  agents to redo committed work would have risked regressions.
- The now-deleted .agents/skills/ralph/prd.json (branchName phase4) listed US-313..US-324;
  those are a renumbered DUPLICATE of the same scope (313=2D overlay=306, 315=movement
  balance=307, etc.), not additional work.
- VERIFICATION (full quality gate, the substance of US-312):
  - pnpm test  -> 33 files, 694 tests passing
  - pnpm typecheck -> 0 errors
  - pnpm lint  -> 0 warnings
  - pnpm build -> optimized production build succeeds (Next.js 16.2.4)
  - Production tree-shaking confirmed: grep of .next/static for "DebugMenu", "MapEditor",
    and "EDITOR MODE" all return 0 hits — dev-only code stripped from production bundle.
- Synced scripts/prd.json: set passes=true for all 16 stories to match reality.
- Remaining manual-only check (cannot run headless here): in-browser FPS/auth/save-load
  round trip via dev-browser per US-312 acceptance criteria. Recommend a quick dev-browser
  pass on /play before tagging the slice shippable.
---
