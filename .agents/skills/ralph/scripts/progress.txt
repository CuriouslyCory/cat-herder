# Ralph Progress Log
Started: Wed Apr 29 05:27:00 PM MST 2026

## Codebase Patterns
- `next/dynamic` with `ssr: false` must live in a `'use client'` component in Next.js 16+ (Turbopack). Server Components cannot use it directly. Pattern: server page → client loader with dynamic import → actual component.
- The main repo is on `main` (Next.js T3 app). Branch `feat/engine-foundation` is a stale worktree with an old Vite-based game. Working branch is `feat/engine-foundation-v2` from main.
- Three.js lives under `src/game/engine/`. Game source is cleanly separated from `src/app/`, `src/server/`, `src/trpc/`.
- Use `pnpm typecheck && pnpm build` as the quality gate (no lint script yet — added in US-019).

---

## 2026-04-29 - US-001
- Installed `three` and `@types/three` via pnpm
- Created `src/game/engine/Game.ts` — minimal Three.js orchestrator (renderer, scene, camera, ground plane, player box, ResizeObserver, rAF loop) with `start()`, `pause()`, `resume()`, `destroy()`
- Created `src/app/play/_components/GameCanvas.tsx` — `'use client'` component that creates a canvas, sizes it to parent, instantiates Game, and calls `game.destroy()` on unmount
- Created `src/app/play/_components/GameLoader.tsx` — `'use client'` wrapper that uses `next/dynamic` with `ssr: false` (required in Next.js 16+, cannot use dynamic in Server Components)
- Updated `src/app/play/page.tsx` — server component that calls `withAuth({ ensureSignedIn: true })`, passes `{ id, firstName, email }` safe user subset to `GameLoader`
- **Learnings for future iterations:**
  - Next.js 16 (Turbopack) rejects `next/dynamic` with `ssr: false` in Server Components. Must wrap in a `'use client'` component.
  - `ResizeObserver` on the canvas element correctly handles resize; `renderer.setSize(w, h, false)` (false = don't set CSS style) is key to keeping pixel-perfect canvas sizing when CSS is handling the layout.
  - Branch `feat/engine-foundation` was already in use by a stale worktree pointing to an old Vite project. Created `feat/engine-foundation-v2` from current `main`.
---
